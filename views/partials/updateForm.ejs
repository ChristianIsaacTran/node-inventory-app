<!-- weapon update form -->

<% if (category === "weapon") { %>
    <form action="/update" method="post">
        <label for=""></label>
        <input type="text">
        <label for=""></label>
        <input type="text">
    </form>


<% } else if(category === "consumable") { %>
   <form action="/update" method="post">

    </form>


<% } else if(category === "utility") { %>
    <form action="/update" method="post">

    </form>
<% } else {%>
    <h1>Error: item category not found.</h1>
<% } %>